9.#include <lpc214x.h>
#include <math.h>
int main()
{
 char str[] = "abcd";
 int i = 0;
 int n = sizeof(str) / sizeof(str[0]);
 for (i = 0; i < n; i++)
 {
 if (str[i] >= 'A' && str[i] <= 'Z')
 {
 str[i] = str[i] + 32;
 }
 else if (str[i] >= 'a' && str[i] <= 'z')
 {
 str[i] = str[i] - 32;
 } } 
 return 0;
}
